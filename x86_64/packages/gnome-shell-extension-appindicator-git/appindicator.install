post_install() {
    echo
    echo "$(tput bold)At the end, reboot and then run: gnome-extensions enable appindicatorsupport@rgcjonas.gmail.com$(tput sgr0)"
    echo
}

post_upgrade() {
    post_install
}

post_remove() {
    echo
    echo "$(tput bold)At the end, run: gnome-extensions uninstall appindicatorsupport@rgcjonas.gmail.com$(tput sgr0)"
    echo
}
